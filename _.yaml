# This file acts as a user config
# It's named _ to put it at the top of the folder
impl: 'https://github.com/Treikeh/blendos-tracks/raw/main'

track: 'hyprland'

packages:
  - 'btop'
  - 'flatpak'
  - 'zed'
  - 'pipewire-alsa' # Bevy
  - 'vulkan-intel'  # Bevy
  - 'rustup'        # Bevy
  - 'clang'         # Bevy
  - 'mold'          # Bevy

#aur-packages:
  #- 'zen-browser-avx2' # Didn't work - Use flatpak
